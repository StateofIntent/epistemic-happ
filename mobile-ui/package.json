{
  "name": "epistemic-mobile-ui",
  "version": "0.1.0",
  "description": "Mobile-responsive web UI (PWA) for practitioners — Claims and Critiques against a live Holochain conductor",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@holochain/client": "^0.17.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "^5.3.0",
    "vite": "^5.4.0"
  },
  "overrides": {
    "libsodium-wrappers": "0.7.13"
  }
}
